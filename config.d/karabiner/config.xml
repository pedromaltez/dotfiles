<?xml version="1.0"?>
<root>
  <item>
    <name>Remap capslock to hyper with escape on tap</name>
    <appendix>
      OS X doesn’t have a hyper key. This maps capslock (actually F19) to control + shift + option/alt + command when held down like a modifier. If it’s merely tapped it sends the code for escape instead.
    </appendix>
    <identifier>caps_lock_to_hyper_or_escape</identifier>
    <autogen>
      --KeyOverlaidModifier--
      KeyCode::F19,

      KeyCode::COMMAND_L,
      ModifierFlag::OPTION_L | ModifierFlag::SHIFT_L | ModifierFlag::CONTROL_L,
      KeyCode::ESCAPE
    </autogen>
  </item>
</root>
